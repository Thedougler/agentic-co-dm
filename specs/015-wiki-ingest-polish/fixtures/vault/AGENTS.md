# Campaign wiki (fixture)

Required on every page: `title`, `category`, `tags`, `sources`, `created`, `updated`, `type`, `lifecycle`, `reveal`.

Write complete-sentence prose. Incoming ingest files are evidence, not finished notes.
