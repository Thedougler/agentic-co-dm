# Performance & Identity Requirements Checklist: Self-Improving Architecture

**Purpose**: Test whether scoped-identity, caching, and timing requirements (FR-024–FR-026, SC-007, US4) are complete, unambiguous, measurable, and consistent with the measurements in plan.md / research R7.
**Created**: 2026-09-24
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [research.md](../research.md) · [data-model.md](../data-model.md) · [contracts/](../contracts/) · [quickstart.md](../quickstart.md) (branch `030-self-improving-architecture` @ 5dbd8e83, constitution v6.0.1)

**Note**: This custom checklist is generated by the `/speckit.checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

**Defaults chosen (non-interactive run, no reviewer available to answer Q1–Q3)**:
- Q1 Depth → Standard, used as a formal gate before `$speckit-tasks`.
- Q2 Audience/timing → Reviewer of spec + plan, before task generation.
- Q3 Focus → five files: `general.md` (cross-cutting), `wiki-cli.md`, `error-ledger.md`, `eval-run.md`, `performance.md`. Must-have items: the six "Spec gaps for Clarify" in plan.md and the measured `wiki health` cold time (82.3 s).
- Owning step for a failing item: spec.md wording → Specify/Clarify; plan.md, research.md, data-model.md, or contracts → Plan.

## Requirement Completeness

- [ ] CHK001 - Does any requirement bound `wiki health` wall time? SC-007 only requires "completes without a harness timeout" and records wall time; the measured cold run is 82.3 s (identity 55.1 s) with no stated ceiling. [Gap, Spec §SC-007, Plan "Measurements taken"]
- [ ] CHK002 - Is the harness timeout that SC-007 depends on identified (which harness, what limit)? e-208 recorded a health timeout at 52 s, yet the 82.3 s run is reported as "no harness timeout applied", so the criterion has no fixed threshold. [Gap, Ambiguity, Spec §SC-007, Spec §Verified surfaces "Scoped identity"]
- [ ] CHK003 - Is a steady-state (warm) `wiki health` target defined separately from the cold, rules-digest-changed worst case (836/837 misses)? [Gap, Research R7]
- [ ] CHK004 - Are the files whose change counts as "relevant" for cache invalidation enumerated (page bodies, frontmatter, `.manifest.json`, lint rules digest, `.vale.ini`, Vale styles/vocabulary)? [Completeness, Spec §FR-025, Data-model §4]
- [ ] CHK005 - Is it specified whether the pairwise-ratio cache (`pairs` keyed by content hashes) is in scope for FR-025, given the spec only names "reusable corpus information"? [Completeness, Spec §FR-025, Research R7]
- [ ] CHK006 - Is the tracking decision for `wiki/_meta/identity-index.json` (tracked vs ignored) made, rather than deferred to "follows `lint-cache.json`"? [Gap, Data-model §4 "Tracking"]

## Requirement Clarity

- [ ] CHK007 - FR-024 lists cheap exact signals as slug, title, aliases, manifest identity only; the plan also admits pages passing the cached character-profile prefilter (> 0.6). Is the prefilter explicitly allowed as a "cheap signal" in the spec? [Gap, Conflict, Spec §FR-024, Plan "Spec gaps" #1, Research R4]
- [ ] CHK008 - Is "manifest identity" defined? The plan reads it as shared provenance with stem ratio > 0.7 or a merge transition. [Ambiguity, Spec §FR-024, Research R4 step 5]
- [ ] CHK009 - Is the same-`type` candidate filter (Research R4 step 5) stated or implied by any requirement, and would it change findings relative to today? [Ambiguity, Spec §FR-024, Spec §FR-026]
- [ ] CHK010 - Is "the maintainer workstation" specified (OS, CPU class, Vale version, vault size) so SC-007's 5 s budget is reproducible? [Ambiguity, Spec §SC-007, Plan "Target Platform"]
- [ ] CHK011 - Is the SC-007 baseline consistent between spec and plan? Spec cites 3.1 s without Vale (d1c3250); plan measured 3.72 s wall with Vale (bf021f03) and states SC-007 is already met. [Consistency, Spec §SC-007, Plan "Measurements taken"]
- [ ] CHK012 - Is "a lint-cache miss" defined precisely (page content change vs rules-digest change), since a rules change makes every page miss? [Clarity, Spec §SC-007, Research R7]

## Acceptance Criteria Quality

- [ ] CHK013 - Is SC-007's "compared count equals selected pages plus their cheap-signal candidates" updated to include prefilter passes, so the criterion matches the plan's `compared = |selected ∪ candidates|`? [Conflict, Spec §SC-007, Plan "Spec gaps" #1, Data-model §4]
- [ ] CHK014 - Does SC-007 state a numeric bound for the unchanged-page rerun, or only cite "measured 0.18 s"? The plan measured 0.28 s. [Measurability, Spec §SC-007, Research R7]
- [ ] CHK015 - Is "findings identical" (FR-025/FR-026) defined as a concrete comparison (which output, which fields, ordering), matching Quickstart V-06? [Measurability, Spec §FR-025, Spec §FR-026, Quickstart V-06]
- [ ] CHK016 - Is US4's objective "time proportional to the pages I selected" turned into a measurable criterion beyond the one-page case? [Measurability, Spec §US4, Spec §SC-007]
- [ ] CHK017 - Is the reference commit for "current whole-vault output" in FR-026 / US4-3 fixed, so parity has a stable baseline? [Clarity, Spec §FR-026, Quickstart V-06]

## Scenario & Edge Case Coverage

- [ ] CHK018 - Are requirements defined for rename and delete of a page that is a candidate for a selected page (stale rows, stale pair ratios)? [Coverage, Spec §FR-025, Data-model §4]
- [ ] CHK019 - Is behavior specified when `.manifest.json` changes but no page changes (index partial refresh vs rebuild)? [Coverage, Data-model §4 "Validity"]
- [ ] CHK020 - Is the corrupt/old-format index edge case tied to a requirement for what the result reports (e.g. index misses, rebuild noted)? [Edge Case, Spec §Edge Cases, Quickstart V-07]
- [ ] CHK021 - Are other whole-vault walkers that FR-024 affects named (plan notes `resolve_identity` for the mutation gate and `_path_for` also walk the vault)? Are they in or out of scope? [Gap, Research R4 "Findings from code"]

## Non-Functional / Dependencies

- [ ] CHK022 - Lint with Vale rewrites tracked `styles/config/vocabularies/CoDM/accept.txt` and `wiki/_meta/lint-cache.json`. Does any requirement say whether a read-side lint or timing run may modify tracked files (compare FR-017 "read-side mutation" and FR-028 "safe bookkeeping")? [Gap, Conflict, Plan "Spec gaps" #6, Research R7]
- [ ] CHK023 - Are creative engine (15.7 s) and Vale (13.1 s) costs in cold health explicitly in or out of scope, rather than out "unless the health baseline shows a harness timeout"? [Ambiguity, Plan "Risks and dependencies"]
- [ ] CHK024 - Is the environment precondition for timing runs (declared deps installed, `OBSIDIAN_VAULT_PATH` or FR-027 discovery) stated as a requirement or assumption rather than only in quickstart? [Assumption, Quickstart "Prerequisites", Plan "Box setup"]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit.implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit.specify` and `/speckit.clarify`
- Markers: `[Gap]` missing requirement, `[Ambiguity]` unclear wording, `[Conflict]` two sources disagree, `[Assumption]` unvalidated premise. `Spec §` = spec.md; `Plan`, `Research R#`, `Data-model §#`, `Contract <name>`, `Quickstart V-##` = plan artifacts
- Items are numbered sequentially for easy reference
