# Evaluation System Requirements Checklist: Self-Improving Architecture

**Purpose**: Test whether the one-evaluator, one-schema, one-result-format requirements (FR-012–FR-019, US2, US3, SC-003–SC-006) and the eval-run contract are complete, unambiguous, and measurable.
**Created**: 2026-09-24
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [research.md](../research.md) · [data-model.md](../data-model.md) · [contracts/](../contracts/) · [quickstart.md](../quickstart.md) (branch `030-self-improving-architecture` @ 5dbd8e83, constitution v6.0.1)

**Note**: This custom checklist is generated by the `/speckit.checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

**Defaults chosen (non-interactive run, no reviewer available to answer Q1–Q3)**:
- Q1 Depth → Standard, used as a formal gate before `$speckit-tasks`.
- Q2 Audience/timing → Reviewer of spec + plan, before task generation.
- Q3 Focus → five files: `general.md` (cross-cutting), `wiki-cli.md`, `error-ledger.md`, `eval-run.md`, `performance.md`. Must-have items: the six "Spec gaps for Clarify" in plan.md and the measured `wiki health` cold time (82.3 s).
- Owning step for a failing item: spec.md wording → Specify/Clarify; plan.md, research.md, data-model.md, or contracts → Plan.

## Requirement Completeness

- [ ] CHK001 - Is `expected_output` required or optional? FR-013 lists it without "optional"; the plan keeps it optional and invents no text. Does the spec say where it comes from for records that lack one? [Gap, Conflict, Spec §FR-013, Plan "Spec gaps" #3, Data-model §2]
- [ ] CHK002 - Does FR-014's "alongside the existing types" list the kept types? The plan keeps `scope`, `handoff`, `coverage` (7 uses) and folds `qualitative`→`quality`, `structural`→`structure`; the spec does not say whether minor types or spelling variants survive. [Gap, Ambiguity, Spec §FR-014, Plan "Spec gaps" #4, Data-model §2]
- [ ] CHK003 - Is the conversion rule for the 143 `expectations[]` records specified in the spec (each string → `{type: behavior, text}`), or only in the data model? [Completeness, Spec §FR-013, Data-model §2]
- [ ] CHK004 - Are the grading semantics of a `behavior` assertion defined (who grades, from what evidence)? [Gap, Spec §FR-014]
- [ ] CHK005 - Is `skill_selected` grading defined for subjects that load more than one `SKILL.md`, or load a non-owner skill first? The spec says "the owner whose `SKILL.md` the subject loads"; the data model says "the first owner `SKILL.md` in `skills_read`". [Ambiguity, Spec §Assumptions, Data-model §3]
- [ ] CHK006 - Is the set of "owners" (FR-003 "and every other owner") enumerated or derivable by a stated rule, so SC-005 "every owner has a passing `skill_selected` eval" has a fixed denominator? [Gap, Spec §FR-003, Spec §SC-005]

## Requirement Clarity

- [ ] CHK007 - Are "retries", "duplicate actions", "completion reason", and "tool calls" defined in the spec? Definitions exist only in research R5 / data-model §3. [Clarity, Spec §FR-016, Research R5]
- [ ] CHK008 - Is "semantic quality" given a scale and grader in the spec? The data model uses a 1–5 rubric from `agents/grader.md`. [Ambiguity, Spec §FR-016, Data-model §3]
- [ ] CHK009 - Is the fallback when codex event names differ from the assumed ones (record `null`, never guess) stated as a requirement, since FR-016 says every metric MUST be present? [Conflict, Spec §FR-016, Research R5 "To verify"]
- [ ] CHK010 - Is "identical in shape" (US2-2, SC-004) defined as the same file set and keys, as the contract states? [Clarity, Spec §SC-004, Contract eval-run]
- [ ] CHK011 - Is removing `run-eval.py`, `run-loop.py`, `improve-description.py`, and skill-creator's "Description optimization" section covered by a requirement, or only by the plan? [Traceability, Spec §FR-012, Spec §FR-015, Research R5]

## Requirement Consistency

- [ ] CHK012 - SC-003 fixes the corpus at "533 eval records", but SC-005 and FR-015 add new `skill_selected` records to the same files. Is the count meant as "all records" rather than a fixed number? [Conflict, Spec §SC-003, Spec §SC-005]
- [ ] CHK013 - Are the metric lists consistent across US3-2 (no latency), FR-016 (latency, no scope), and US8-1 (adds scope)? Is "scope" as a metric defined anywhere? [Consistency, Spec §US3, Spec §FR-016, Spec §US8]
- [ ] CHK014 - Is the stale-behavior list consistent across US2-3, FR-017, and SC-003 ("outdated completion rules" and "obsolete approval flows" appear in FR-017 but not SC-003)? [Consistency, Spec §FR-017, Spec §SC-003]

## Acceptance Criteria Quality

- [ ] CHK015 - Can SC-003's "0 records assert Work-gate ... behavior" be checked objectively? Quickstart V-09 greps wording, but the spec's Edge Case keeps current behavior with retired wording updated. [Measurability, Spec §SC-003, Quickstart V-09]
- [ ] CHK016 - Is the model and effort for the FR-019 baseline pinned so later SC-012 comparisons use the same configuration? [Gap, Spec §FR-019, Spec §SC-012, Spec §Assumptions (XXVI)]
- [ ] CHK017 - Does SC-006 require at least one case per category and at least ten total, consistent with US3-1 "at least one recorded case" per category? [Consistency, Spec §SC-006, Spec §US3]
- [ ] CHK018 - Is FR-018's "current enforcing surface" defined for each absent executable, or is choosing it left to implementation? [Clarity, Spec §FR-018, Plan "026 references"]

## Scenario & Edge Case Coverage

- [ ] CHK019 - Is the usage-limit resume rule (skip run dirs with `timing.json` exit 0) specified as a requirement, not just a contract note? [Coverage, Spec §Edge Cases, Contract eval-run]
- [ ] CHK020 - Is the case of a two-owner-plausible skill-selection eval tied to a requirement on the skills' ownership text (fix the skill, not the eval)? [Coverage, Spec §Edge Cases, Spec §FR-003]
- [ ] CHK021 - Is validation of `skill_selected.text` (must be an existing directory under `.agents/skills/`) specified, and what happens when a skill is renamed? [Edge Case, Data-model §2]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit.implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit.specify` and `/speckit.clarify`
- Markers: `[Gap]` missing requirement, `[Ambiguity]` unclear wording, `[Conflict]` two sources disagree, `[Assumption]` unvalidated premise. `Spec §` = spec.md; `Plan`, `Research R#`, `Data-model §#`, `Contract <name>`, `Quickstart V-##` = plan artifacts
- Items are numbered sequentially for easy reference
