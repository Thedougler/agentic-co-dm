# General Requirements Quality Checklist: Self-Improving Architecture

**Purpose**: Cross-cutting test of the spec as a whole: loop and friction rule, scope boundaries, promotion and small-skill requirements (FR-001–FR-005, FR-040–FR-045, US5, US8, US9), success-criteria measurability, and consistency between spec.md and plan artifacts. Domain detail lives in `wiki-cli.md`, `error-ledger.md`, `eval-run.md`, `performance.md`.
**Created**: 2026-09-24
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [research.md](../research.md) · [data-model.md](../data-model.md) · [contracts/](../contracts/) · [quickstart.md](../quickstart.md) (branch `030-self-improving-architecture` @ 5dbd8e83, constitution v6.0.1)

**Note**: This custom checklist is generated by the `/speckit.checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

**Defaults chosen (non-interactive run, no reviewer available to answer Q1–Q3)**:
- Q1 Depth → Standard, used as a formal gate before `$speckit-tasks`.
- Q2 Audience/timing → Reviewer of spec + plan, before task generation.
- Q3 Focus → five files: `general.md` (cross-cutting), `wiki-cli.md`, `error-ledger.md`, `eval-run.md`, `performance.md`. Must-have items: the six "Spec gaps for Clarify" in plan.md and the measured `wiki health` cold time (82.3 s).
- Owning step for a failing item: spec.md wording → Specify/Clarify; plan.md, research.md, data-model.md, or contracts → Plan.

## Requirement Completeness

- [ ] CHK001 - Is "friction" given a test an agent can apply at runtime (what makes a task "unnecessarily difficult")? The Key Entities list gives categories only. [Ambiguity, Spec §Key Entities "Friction", Spec §FR-002]
- [ ] CHK002 - Is the threshold for "fix is larger than the original task" defined, so agents can decide between repairing and recording? [Gap, Spec §Edge Cases, Spec §FR-006]
- [ ] CHK003 - Does FR-045 define "repeated recorded evidence" (count, window, source)? [Ambiguity, Spec §FR-045]
- [ ] CHK004 - Are "non-inferior" (FR-041, FR-044, SC-012, US9-2) and "measurable reduction" (SC-012) quantified with a margin, sample size, or run count? [Measurability, Gap, Spec §FR-044, Spec §SC-012]
- [ ] CHK005 - Is "cross-skill case" (Edge Case: promotion refused on cross-skill regression) tied to a defined set, e.g. the FR-019 baseline? [Clarity, Spec §Edge Cases, Spec §FR-042]
- [ ] CHK006 - Are requirements defined for how "scope" and "narrower scope" are measured in promotion ranking? [Gap, Spec §FR-044, Spec §US8]

## Requirement Clarity

- [ ] CHK007 - Is "smallest source change" (FR-002) defined enough to be reviewable? [Ambiguity, Spec §FR-002]
- [ ] CHK008 - Is FR-005's "committed" consistent with work that ends without a commit (read-only tasks, blocked tasks)? [Clarity, Spec §FR-005]
- [ ] CHK009 - Is SC-013 "no longer than before" measured in a stated unit (lines, tokens) against a stated commit? [Measurability, Spec §SC-013, Quickstart V-19]
- [ ] CHK010 - Is "seeded-friction trial" setup in SC-011 (friction kinds, subject model, task pool) specified so the 4/5 threshold is reproducible? [Measurability, Spec §SC-011, Quickstart V-17]

## Requirement Consistency

- [ ] CHK011 - US1 "Why this priority" says "Five open ledger entries" but lists nine ids (e-205, e-207, e-209, e-211, e-212, e-214–e-216, e-218). Is the count corrected? [Consistency, Spec §US1]
- [ ] CHK012 - The Assumptions still call the XIII amendment a blocking prerequisite not yet done; the constitution is now v6.0.1 and the plan marks it met. Is spec status aligned? [Conflict, Spec §Assumptions "Dependency", Plan "Constitution Check"]
- [ ] CHK013 - Is "Out of scope: edits to `.specify/memory/constitution.md`" consistent with the plan's reliance on v6.0.0/v6.0.1 landed outside this feature? [Consistency, Spec §Classification and Scope, Plan "Risks and dependencies"]
- [ ] CHK014 - FR-017 bans read-side mutation as asserted behavior, while lint (a read-side check) rewrites tracked `accept.txt` and `lint-cache.json`. Is the spec's position on this side effect stated? [Conflict, Spec §FR-017, Plan "Spec gaps" #6]
- [ ] CHK015 - Are FR-001 ("no separate self-improvement ... command") and FR-043 ("MUST NOT add a runtime system") consistent with the plan's new `error migrate` and `error recurrence` verbs and `metrics.json` file? [Consistency, Spec §FR-001, Spec §FR-043, Contract error-ledger]

## Acceptance Criteria Quality

- [ ] CHK016 - Does every FR have a success criterion or acceptance scenario that verifies it? FR-004, FR-005, FR-023, FR-029, FR-040–FR-042, FR-045 have no direct SC. [Coverage, Traceability, Spec §Requirements, Spec §Success Criteria]
- [ ] CHK017 - Is SC-002 "across all page kinds in `wiki/`" enumerable (list of kinds or a rule)? [Measurability, Spec §SC-002]
- [ ] CHK018 - Is SC-012's comparison baseline ("against the SC-006 baseline") consistent with FR-043's incumbent-vs-candidate on the same evals (not the 029 baseline cases)? [Consistency, Spec §SC-012, Spec §FR-043]

## Scenario Coverage

- [ ] CHK019 - Are recovery requirements defined for a failed or partial promotion (candidate applied, evals later regress)? [Gap, Recovery, Spec §FR-041]
- [ ] CHK020 - Are rollback requirements defined for the one-time corpus conversion and ledger migration if a check fails mid-way? [Gap, Recovery, Spec §FR-010, Spec §FR-013]
- [ ] CHK021 - Is the external-source friction case (harness binary, external package) connected to a rule for when to block vs continue (per "the owner guard")? [Coverage, Spec §Edge Cases]

## Dependencies & Assumptions

- [ ] CHK022 - Is the binding phase order enforced by a stated gate (e.g. Phase 2 starts only after SC-001–SC-008 pass)? [Assumption, Spec §Assumptions]
- [ ] CHK023 - Is the `cli-for-agents` guidance, which is not a repository file, fully captured by FR-030–FR-039 so no requirement depends on an external document? [Dependency, Spec §Verified surfaces]
- [ ] CHK024 - Are the plan's "Spec gaps for Clarify" 1–6 each either resolved in spec.md or tracked in a checklist item here or in the domain checklists? (#1 performance CHK007/CHK013; #2 error-ledger CHK001; #3 eval-run CHK001; #4 eval-run CHK002; #5 error-ledger CHK013; #6 performance CHK022.) [Traceability, Plan "Spec gaps for Clarify"]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit.implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit.specify` and `/speckit.clarify`
- Markers: `[Gap]` missing requirement, `[Ambiguity]` unclear wording, `[Conflict]` two sources disagree, `[Assumption]` unvalidated premise. `Spec §` = spec.md; `Plan`, `Research R#`, `Data-model §#`, `Contract <name>`, `Quickstart V-##` = plan artifacts
- Items are numbered sequentially for easy reference
