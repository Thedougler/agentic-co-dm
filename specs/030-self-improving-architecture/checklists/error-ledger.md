# Error Ledger Requirements Checklist: Self-Improving Architecture

**Purpose**: Test whether the `errors.md` format, matching, draining, migration, and recurrence requirements (FR-006–FR-011, US5-3, US6, SC-009) and the error-ledger contract are complete, unambiguous, and measurable.
**Created**: 2026-09-24
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [research.md](../research.md) · [data-model.md](../data-model.md) · [contracts/](../contracts/) · [quickstart.md](../quickstart.md) (branch `030-self-improving-architecture` @ 5dbd8e83, constitution v6.0.1)

**Note**: This custom checklist is generated by the `/speckit.checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

**Defaults chosen (non-interactive run, no reviewer available to answer Q1–Q3)**:
- Q1 Depth → Standard, used as a formal gate before `$speckit-tasks`.
- Q2 Audience/timing → Reviewer of spec + plan, before task generation.
- Q3 Focus → five files: `general.md` (cross-cutting), `wiki-cli.md`, `error-ledger.md`, `eval-run.md`, `performance.md`. Must-have items: the six "Spec gaps for Clarify" in plan.md and the measured `wiki health` cold time (82.3 s).
- Owning step for a failing item: spec.md wording → Specify/Clarify; plan.md, research.md, data-model.md, or contracts → Plan.

## Requirement Completeness

- [ ] CHK001 - Does the spec define when two causes are "the same" for FR-007? The plan matches equal `source` plus a normalized `cause_key` (casefold, collapse whitespace, strip digits and quoted literals) and adds `--attach e-N` to force a match; neither appears in the spec. [Gap, Ambiguity, Spec §FR-007, Plan "Spec gaps" #2, Research R3]
- [ ] CHK002 - Is the allowed form of `source` specified in the spec (repo-relative path vs component name vs `external:<name>`)? The data model defines it; FR-006 says only "authoritative file or component". [Clarity, Spec §FR-006, Data-model §1, Contract error-ledger]
- [ ] CHK003 - Is "reusable" in "reusable, unresolved defects" defined with a test an agent can apply? [Ambiguity, Spec §FR-006]
- [ ] CHK004 - Is the `detail` field's content specified (free text, command output, default to cause)? The contract defaults it to the cause text. [Gap, Spec §FR-006, Contract error-ledger]
- [ ] CHK005 - Is the hand-assigned `source` for the non-merged open entries (e-168, e-201, e-202, e-203, e-210, e-217) required to be set by a rule, or only "by hand ... taken from their cause text"? [Completeness, Spec §FR-010, Research R3 "Migration"]
- [ ] CHK006 - Are the migration trigger and its idempotence specified in the spec? FR-010 says "on first run of the new format"; the plan adds an explicit `error migrate` command. [Consistency, Spec §FR-010, Contract error-ledger]

## Requirement Clarity & Consistency

- [ ] CHK007 - Is keeping `--cause-fixed true` as an accepted no-op consistent with FR-006 removing `cause_fixed` and FR-030/FR-035 strict input handling? [Consistency, Spec §FR-006, Research R3]
- [ ] CHK008 - Are the merge groups in FR-010 and the surviving id (lowest id: e-205, e-212, e-206) stated in the spec, or only in research? [Clarity, Spec §FR-010, Research R3]
- [ ] CHK009 - Does FR-008 ("drain in the same change that lands the fix") cover defects whose source is external (`external:<name>`) and can never be fixed in-repo? [Coverage, Spec §FR-008, Spec §Edge Cases, Data-model §1]
- [ ] CHK010 - Is the "verified" in "verified fix" (FR-008, US6-2) tied to FR-011's regression case, so draining has an objective precondition? [Clarity, Spec §FR-008, Spec §FR-011]
- [ ] CHK011 - Is "an identical occurrence already present changes nothing" (contract) defined: identical on `sitting` alone, or `sitting` + `detail`? [Ambiguity, Contract error-ledger, Spec §FR-036]
- [ ] CHK012 - Is the spec's Edge Case "same-root-cause requires the same `source`" consistent with `--attach e-N`, which can attach across sources? [Conflict, Spec §Edge Cases, Research R3]

## Acceptance Criteria Quality

- [ ] CHK013 - Is SC-009's 20-sitting window justified or marked as a provisional default to be revisited? The spec labels it an assumption; no data sets it. [Assumption, Spec §SC-009, Spec §Assumptions, Plan "Spec gaps" #5]
- [ ] CHK014 - Is "the 20 sittings following Phase 2" anchored to a countable event (first `sittings.jsonl` label after a named commit)? [Measurability, Spec §SC-009]
- [ ] CHK015 - Does the recurrence metric (occurrences after the first on open entries) stay meaningful once entries are drained, since drained occurrences vanish from the count? Is that loss accepted in the spec? [Clarity, Spec §FR-009, Spec §Clarifications Q4]
- [ ] CHK016 - Is FR-011's "fails before the fix" defined for fixes to non-executable sources (instruction text, eval records), where "before" cannot be rerun after the fact? [Measurability, Spec §FR-011]

## Scenario & Edge Case Coverage

- [ ] CHK017 - Are requirements defined for `append` when two open entries both match (possible after normalization), or when `--attach` names a missing id? [Edge Case, Gap, Contract error-ledger]
- [ ] CHK018 - Is the case of a friction whose fix is larger than the task (record it, finish by the sanctioned path) linked to FR-006's "fixed and verified in the same task MUST NOT create an entry"? [Coverage, Spec §Edge Cases, Spec §FR-006]
- [ ] CHK019 - Are concurrent writers to `errors.md` (parallel agents on the shared box) addressed, or explicitly out of scope? [Gap]
- [ ] CHK020 - Is the invariant "no two entries share `(source, cause_key)`" stated as a requirement that `list`/`migrate` report violations of, or only as a data-model note? [Completeness, Data-model §1, Spec §SC-009]

## Dependencies & Assumptions

- [ ] CHK021 - The spec's Assumptions still describe the constitution XIII amendment as a pending, blocking dependency ("5.0.0 → 6.0.0"); the plan says v6.0.0/v6.0.1 satisfies it. Is the spec's dependency text current? [Consistency, Spec §Assumptions "Dependency", Plan "Constitution Check" XIII]
- [ ] CHK022 - Are the guidance rewrites that FR-006–FR-008 require (`AGENTS.md` "Error ledger" line ~196, `wiki-lint/SKILL.md` line ~47) named in the spec, or only in the plan? [Traceability, Plan "Phase 2"]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit.implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit.specify` and `/speckit.clarify`
- Markers: `[Gap]` missing requirement, `[Ambiguity]` unclear wording, `[Conflict]` two sources disagree, `[Assumption]` unvalidated premise. `Spec §` = spec.md; `Plan`, `Research R#`, `Data-model §#`, `Contract <name>`, `Quickstart V-##` = plan artifacts
- Items are numbered sequentially for easy reference
