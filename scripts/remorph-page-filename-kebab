#!/usr/bin/env python3
"""Deprecated alias — use ./scripts/remorph-page-filenames."""
import runpy
import sys
from pathlib import Path

target = Path(__file__).with_name("remorph-page-filenames")
sys.argv[0] = str(target)
runpy.run_path(str(target), run_name="__main__")
