[project]
name = "agentic-co-dm"
version = "0.1.0"
description = "Campaign wiki checks for Agentic Co-DM. Runtime: tiktoken for objective token counts."
readme = "CONTEXT.md"
requires-python = ">=3.12"
dependencies = [
    "tiktoken>=0.7,<1",
    "PyYAML>=6,<7",
]

[tool.agentic-co-dm]
vault = "wiki"
