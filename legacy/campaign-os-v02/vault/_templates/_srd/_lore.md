---
type: lore
status: draft
publish: false
title: ""                      # OPTIONAL — display title if it differs from the H1 heading; absent ⇒ the H1 is the title (llm-wiki skill)
aliases: []
summary: ""                    # one sentence, ≤200 chars — cheap page preview, never empty
created: "{date}"
updated: "{date}"
owner_skill: ".claude/skills/draft-content/references/lore.md"   # OPTIONAL — the guide or skill that owns this page's quality
tags: []
tier: supporting        # OPTIONAL — core | supporting | peripheral; absent ⇒ supporting (llm-wiki skill)
subtype: fact           # fact | legend — set the real subtype; fact is a placeholder. A legend is a fact with multiple conflicting in-world accounts (state each, and which the DM treats as ground truth if any)
edit_pass: 0            # OPTIONAL — last completed line pass (0–3); the draft-story skill is this key's sole writer
campaigns: []            # OPTIONAL — this page's campaign(s), if this repo runs more than one
uid: 4a173458-7201-4a75-82c2-727b98773ad4
---

# <Name>

*One-line description — what this lore covers, in one sentence.*

## The Fact

The fact stated plainly, point-first — what's actually true, however
different from what's commonly believed (state the common belief too,
and flag the gap, if any). Names what's currently active about this
fact — a countdown, a custom about to be tested, a debt coming due — or
says explicitly that it's atmospheric texture with no hook; never left
ambiguous. States which PC's thread this pulls on, or the DM's explicit
call that it's foundational world-texture with none — never a silent
default. H3 subsections only if the content genuinely needs them (a
civilization's origin and fall, a prophecy's stages). Growing the body
from raw notes or fragments: load the `draft-story` skill. Sharpening the
questions the fact should raise: refer to craft guidance in `vault/refs/ideas/`.

### Deliberate Silences <!-- OPTIONAL: only when this lore intentionally leaves something unexplained (a mystery with no answer, an ambiguity the table should never resolve) -->

One line per item — see `vault/refs/stories/reserved-lines-and-silences.md` for how a
checker treats what's listed here.
