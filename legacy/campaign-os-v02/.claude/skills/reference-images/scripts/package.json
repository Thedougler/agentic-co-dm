{
  "name": "@shattered-sea/reference-images",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Resolver for the reference-images skill — reads a page's reference_image frontmatter and validates it to an absolute path.",
  "scripts": {
    "test": "node --test"
  }
}
