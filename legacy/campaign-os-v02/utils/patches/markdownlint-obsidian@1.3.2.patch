diff --git a/dist/src/application/LintUseCase.js b/dist/src/application/LintUseCase.js
index 5adb3e04bcda956a99c4a71b58609750f13b3414..61e8d8ee9cf2d7bdd4c55fbbb4ed8bfc27f3d9d7 100644
--- a/dist/src/application/LintUseCase.js
+++ b/dist/src/application/LintUseCase.js
@@ -64,17 +64,24 @@ function iterateActiveRules(registry, config) {
     });
 }
 async function runRule(rule, parsed, config, vault, blockRefIndex, fsCheck, errors) {
+    // Body rules see frontmatter-stripped content, so their line numbers are
+    // body-relative; frontmatter-tagged rules already report absolute lines.
+    // Offset here — the single choke point — so reported lines and Fix
+    // payloads both match the on-disk file that applyFixes() splices.
+    const lineOffset = rule.tags.includes("frontmatter") ? 0 : parsed.frontmatterEndLine;
     await rule.run({ filePath: parsed.filePath, parsed, config, vault, fsCheck, blockRefIndex }, (partial) => {
         warnIfMissingFix(rule, partial.fix);
         errors.push(makeLintError({
             ruleCode: rule.names[0] ?? "UNKNOWN",
             ruleName: rule.names[1] ?? rule.names[0] ?? "unknown",
             severity: rule.severity,
-            line: partial.line,
+            line: partial.line + lineOffset,
             column: partial.column,
             message: partial.message,
             fixable: rule.fixable,
-            ...(partial.fix !== undefined ? { fix: partial.fix } : {}),
+            ...(partial.fix !== undefined
+                ? { fix: { ...partial.fix, lineNumber: partial.fix.lineNumber + lineOffset } }
+                : {}),
         }));
     });
 }
diff --git a/dist/src/domain/fix/applyFixes.js b/dist/src/domain/fix/applyFixes.js
index 463bb0081c5cebd3b655275887494d9d828ac344..86316c0843b0103afb16f09f4279d34c13d489e7 100644
--- a/dist/src/domain/fix/applyFixes.js
+++ b/dist/src/domain/fix/applyFixes.js
@@ -7,18 +7,46 @@
  * column ranges the later one (lower editColumn) is skipped and recorded in
  * the returned `conflicts` array.
  *
+ * A fix with `deleteLine: true` removes that whole line from the file instead
+ * of splicing its content — these are applied last, in descending line-number
+ * order, so an earlier removal never shifts the index a later one targets. A
+ * line carrying both a content-splice fix and a `deleteLine` fix is a
+ * conflict (editing content on a line about to disappear is incoherent) — the
+ * deletion is skipped and recorded, the content edit still applies.
+ *
  * All column numbers are 1-based, matching Fix / LintError conventions.
  */
 export function applyFixes(raw, fixes, filePath = "") {
     // Split on \n only — trailing \r on each line is preserved naturally
     const lines = raw.split("\n");
     const conflicts = [];
-    const byLine = groupByLine(fixes);
+    const lineDeletions = fixes.filter((fix) => fix.deleteLine === true);
+    const editFixes = fixes.filter((fix) => fix.deleteLine !== true);
+    const byLine = groupByLine(editFixes);
     for (const [lineNumber, list] of byLine.entries()) {
         applyLineGroup(lines, lineNumber, list, filePath, conflicts);
     }
+    applyLineDeletions(lines, lineDeletions, byLine, filePath, conflicts);
     return { patched: lines.join("\n"), conflicts };
 }
+function applyLineDeletions(lines, lineDeletions, byLine, filePath, conflicts) {
+    const targets = [...new Set(lineDeletions.map((fix) => fix.lineNumber))]
+        .filter((lineNumber) => {
+            if (!byLine.has(lineNumber))
+                return true;
+            conflicts.push({
+                filePath,
+                first: lineDeletions.find((fix) => fix.lineNumber === lineNumber),
+                second: byLine.get(lineNumber)[0],
+                reason: `Line ${lineNumber} has both a content edit and a deleteLine fix`,
+            });
+            return false;
+        })
+        .sort((a, b) => b - a);
+    for (const lineNumber of targets) {
+        lines.splice(lineNumber - 1, 1);
+    }
+}
 function groupByLine(fixes) {
     const byLine = new Map();
     for (const fix of fixes) {
diff --git a/dist/src/domain/linting/Fix.js b/dist/src/domain/linting/Fix.js
index 6bdc426f5175df436fa3c3b93df9baf048cbeb8d..feeffdf1e0b5bc7452e87e0cdbc4766e94850b26 100644
--- a/dist/src/domain/linting/Fix.js
+++ b/dist/src/domain/linting/Fix.js
@@ -10,6 +10,9 @@
 export function makeFix(fields) {
     if (fields.lineNumber < 1)
         throw new Error("Fix.lineNumber must be >= 1");
+    if (fields.deleteLine === true) {
+        return Object.freeze({ ...fields });
+    }
     if (fields.editColumn < 1)
         throw new Error("Fix.editColumn must be >= 1");
     if (fields.deleteCount < 0)
diff --git a/dist/src/infrastructure/discovery/FileDiscovery.js b/dist/src/infrastructure/discovery/FileDiscovery.js
index 336ac45f86598230d048733638b4df65939e8e29..f0c7fe115244d30bbc639ec81d87bfff4d4c1455 100644
--- a/dist/src/infrastructure/discovery/FileDiscovery.js
+++ b/dist/src/infrastructure/discovery/FileDiscovery.js
@@ -28,7 +28,14 @@ export async function discoverFiles(globs, ignores, cwd) {
         absolute: true,
         gitignore: true,
         dot: false,
+        // A bare directory glob (e.g. "content/craft/", no "**/*.md" suffix)
+        // makes globby's expandDirectories default sweep every file inside,
+        // extension-agnostic — binary siblings like exemplars/*.pdf then get
+        // parsed as text and flood output with garbage tag/heading findings.
+        // This linter only ever means to lint Markdown, so filter here once,
+        // independent of what pattern the caller passed.
+        expandDirectories: { extensions: ["md"] },
     });
-    return files.sort();
+    return files.filter((f) => f.endsWith(".md")).sort();
 }
 //# sourceMappingURL=FileDiscovery.js.map
\ No newline at end of file
diff --git a/dist/src/infrastructure/rules/ofm/embeds/OFM020-broken-embed.js b/dist/src/infrastructure/rules/ofm/embeds/OFM020-broken-embed.js
index 63800d5925209ba1b60bb2d5eefebf363115324d..91312525d7cb49d8584d9dea52d464f933943b15 100644
--- a/dist/src/infrastructure/rules/ofm/embeds/OFM020-broken-embed.js
+++ b/dist/src/infrastructure/rules/ofm/embeds/OFM020-broken-embed.js
@@ -8,6 +8,13 @@ import { classifyEmbed } from "./shared/EmbedClassifier.js";
  * vault index is `null` (resolve disabled) this rule is a no-op, matching
  * the OFM001 convention.
  *
+ * A file under a `_templates/` directory whose embed target contains `<` or
+ * `>` is a scaffold placeholder (e.g. `[[<slug>-statblock]]`,
+ * `[[<encounter-page>]]`) filled in at instantiation time — never a real,
+ * resolvable target, so it is not rot. That check is scoped to
+ * `_templates/` only: a genuinely broken target with no placeholder syntax,
+ * in `_templates/` or anywhere else, still fires.
+ *
  * @see docs/rules/embeds/OFM020.md
  */
 export const OFM020Rule = {
@@ -19,10 +26,13 @@ export const OFM020Rule = {
     run({ parsed, vault }, onError) {
         if (vault === null)
             return;
+        const inTemplates = /(^|\/)_templates\//.test(parsed.filePath.replace(/\\/g, "/"));
         for (const embed of parsed.embeds) {
             const { kind } = classifyEmbed(embed);
             if (kind !== "markdown")
                 continue;
+            if (inTemplates && /[<>]/.test(embed.target))
+                continue;
             const match = vault.resolve({ target: embed.target });
             if (match.kind === "not-found") {
                 onError({
diff --git a/dist/src/infrastructure/rules/ofm/wikilinks/OFM001-broken-wikilink.js b/dist/src/infrastructure/rules/ofm/wikilinks/OFM001-broken-wikilink.js
index 9fcda5efe6d5704fab021adaf0766ee8f0bffa36..62baff31f0cffe96f0b4268321b910c2bad21dc6 100644
--- a/dist/src/infrastructure/rules/ofm/wikilinks/OFM001-broken-wikilink.js
+++ b/dist/src/infrastructure/rules/ofm/wikilinks/OFM001-broken-wikilink.js
@@ -7,6 +7,13 @@
  * vault index is `null` (e.g. `--no-resolve` or `resolve: false`) this rule
  * does nothing.
  *
+ * A file under a `_templates/` directory whose wikilink target contains `<`
+ * or `>` is a scaffold placeholder (e.g. `[[<slug>]]`, `[[moment-<N>-<slug>]]`)
+ * filled in at instantiation time — never a real, resolvable target, so it is
+ * not rot. That check is scoped to `_templates/` only: a genuinely broken
+ * target with no placeholder syntax, in `_templates/` or anywhere else,
+ * still fires.
+ *
  * @see docs/rules/wikilinks/OFM001.md
  */
 export const OFM001Rule = {
@@ -18,9 +25,12 @@ export const OFM001Rule = {
     run({ parsed, vault }, onError) {
         if (vault === null)
             return;
+        const inTemplates = /(^|\/)_templates\//.test(parsed.filePath.replace(/\\/g, "/"));
         for (const link of parsed.wikilinks) {
             if (link.isEmbed)
                 continue;
+            if (inTemplates && /[<>]/.test(link.target))
+                continue;
             const match = vault.resolve(link);
             if (match.kind === "not-found") {
                 onError({
diff --git a/dist/src/infrastructure/rules/standard/StandardRuleAdapter.js b/dist/src/infrastructure/rules/standard/StandardRuleAdapter.js
index 95e9ce51acb223a1b7c3333ea089cf541ab5939b..b79f3c5abd7732a6b9e41b51c4e2c58e825f39a0 100644
--- a/dist/src/infrastructure/rules/standard/StandardRuleAdapter.js
+++ b/dist/src/infrastructure/rules/standard/StandardRuleAdapter.js
@@ -4,18 +4,15 @@ import { makeFix } from "../../../domain/linting/Fix.js";
  * filling in defaults for any fields the library left absent.
  *
  * Markdownlint uses `deleteCount: -1` as a sentinel for "delete the
- * entire line, including its trailing newline" (see MD012, MD053). Our
- * column-based {@link applyFixes} machinery cannot represent line
- * removal, so we treat the violation as un-fixable and return
- * `undefined`: the diagnostic still surfaces, but no autofix is
- * attached. Without this guard, `makeFix` would throw and the lint pass
- * would surface as `OFM901: Fix.deleteCount must be >= 0` against the
- * file root — issue #28.
+ * entire line, including its trailing newline" (see MD012, MD053) — our
+ * {@link applyFixes} machinery represents that directly via the
+ * `deleteLine` fix kind, so the sentinel maps straight across.
  */
 function fixInfoToFix(fi, fallbackLine) {
     const rawDelete = fi.deleteCount ?? 0;
-    if (rawDelete < 0)
-        return undefined;
+    if (rawDelete < 0) {
+        return makeFix({ lineNumber: fi.lineNumber ?? fallbackLine, deleteLine: true });
+    }
     return makeFix({
         lineNumber: fi.lineNumber ?? fallbackLine,
         editColumn: fi.editColumn ?? 1,
