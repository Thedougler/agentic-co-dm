---
type: npc
tags: [boss-fight, combat]
status: canon
---

# Alias Canon

Body prose. `status: canon` — W113 still detects the `boss-fight` alias but
must never mark the finding fixable on a canon page. The page also carries
the literal `combat` Domain tag, so W111 stays silent.
